# 🍽️ 食堂菜单展示系统

现代化的食堂菜单展示系统，支持四季主题切换和开门红主题，内置后台管理功能。使用 Cloudflare Pages 和 D1 数据库构建。

## ✨ 功能特性

- 📺 **超宽屏展示** - 专为3200×192长条形显示屏设计
- 🎨 **多主题支持** - 春夏秋冬四季主题 + 开门红节日主题  
- ⏰ **实时时钟** - 显示当前日期、时间和星期
- 🔄 **自动刷新** - 菜单数据每30秒自动同步
- 🔐 **后台管理** - 简单高效的菜单编辑和管理
- 💾 **预存菜单** - 可保存多套菜单方案

## 🚀 快速部署

### 1. 安装依赖
```bash
npm install
```

### 2. 登录 Cloudflare
```bash
npx wrangler login
```

### 3. 创建数据库
```bash
npx wrangler d1 create canteen_menu_db
```

### 4. 初始化数据库
```bash
npx wrangler d1 execute canteen_menu_db --file=database/schema.sql
```

### 5. 部署
```bash
npm run deploy
```

### 6. 绑定数据库
在 Cloudflare Dashboard 中：Settings → Functions → D1 database bindings，添加 DB 绑定。

## 📁 项目结构

```
canteen-menu-system/
├── public/                 # 前端静态资源
│   ├── index.html         # 菜单展示页面
│   ├── css/style.css      # 展示页样式
│   ├── js/
│   │   ├── themes.js      # 主题配置
│   │   └── display.js     # 展示页逻辑
│   └── admin/             # 后台管理
│       ├── login.html
│       ├── index.html
│       ├── css/admin.css
│       └── js/admin.js
├── functions/api/          # Cloudflare Pages Functions
├── database/schema.sql      # 数据库结构
├── package.json           # 项目配置
└── wrangler.toml         # Cloudflare 配置
```

## 🎨 主题系统

- **春季主题** - 清新粉绿色调
- **夏季主题** - 清凉蓝色色调  
- **秋季主题** - 温暖橙黄色调
- **冬季主题** - 优雅紫色色调
- **开门红主题** - 喜庆红金色调（默认）

## 🔧 使用说明

### 菜单展示
访问部署地址查看菜单显示

### 后台管理  
1. 访问 `/admin/login.html`
2. 默认账号：admin / admin123
3. 编辑菜单、切换主题

### 修改密码
```bash
npx wrangler d1 execute canteen_menu_db --command="UPDATE users SET password='新密码' WHERE username='admin'"
```

## 📄 许可证

MIT License