# 食堂菜单展示系统

一个现代化的食堂菜单管理系统，支持大屏幕显示和后台管理，适用于学校、企业等食堂场景。

## 🌟 主要功能

### 前台展示
- **大屏幕适配**: 专为3200×192分辨率设计的超宽屏幕显示
- **实时更新**: 支持菜单数据的实时更新和缓存控制
- **多主题支持**: 内置多种精美主题，支持自定义背景
- **响应式设计**: 支持不同屏幕尺寸的自适应显示
- **菜品标签**: 支持畅销、推荐等个性化标签显示

### 后台管理
- **分类管理**: 支持默认分类和自定义分类的增删改查
- **菜品管理**: 完整的菜品管理功能，包括价格和标签设置
- **主题切换**: 实时切换显示主题和背景
- **数据预存**: 支持菜单数据的保存和快速应用
- **实时预览**: 管理界面实时预览显示效果

## 🚀 技术栈

- **前端**: HTML5, CSS3, JavaScript (ES6+)
- **后端**: Cloudflare Workers
- **数据库**: Cloudflare D1 (SQLite)
- **部署**: Cloudflare Pages/Workers
- **样式**: CSS Grid, Flexbox, CSS Variables

## 📁 项目结构

```
canteen-menu-system/
├── README.md                 # 项目说明文档
├── package.json              # 项目依赖配置
├── wrangler.toml            # Cloudflare Workers配置
├── deploy.ps1               # 部署脚本
├── database/                # 数据库相关
│   └── schema.sql           # 数据库表结构
├── functions/               # 云函数
│   └── api/
│       └── [[path]].js      # API路由处理
└── public/                  # 前端静态文件
    ├── index.html           # 前台展示页面
    ├── admin/               # 后台管理界面
    │   ├── index.html      # 管理主页
    │   ├── css/
    │   │   └── admin.css   # 管理界面样式
    │   └── js/
    │       └── admin.js    # 管理界面逻辑
    ├── css/
    │   └── style.css       # 前台展示样式
    └── js/
        ├── display.js      # 展示页面逻辑
        └── themes.js       # 主题配置
```

## 🛠️ 安装与部署

### 本地开发

1. **克隆项目**
```bash
git clone <repository-url>
cd canteen-menu-system
```

2. **安装依赖**
```bash
npm install
```

3. **配置环境变量**
创建 `.dev.vars` 文件：
```
D1_DATABASE=your_database_name
ADMIN_PASSWORD=your_admin_password
```

4. **启动开发服务器**
```bash
npx wrangler dev
```

### 生产部署

1. **配置Wrangler**
编辑 `wrangler.toml` 文件，设置你的项目名称和数据库配置。

2. **创建D1数据库**
```bash
npx wrangler d1 create canteen-menu-db
```

3. **执行数据库迁移**
```bash
npx wrangler d1 migrations apply canteen-menu-db --remote
```

4. **部署项目**
```bash
npm run deploy
```

或使用PowerShell脚本（Windows）：
```powershell
./deploy.ps1
```

## 📊 数据库设计

### 主要表结构

#### 菜单表 (menus)
- `id`: 主键
- `date`: 日期
- `theme`: 主题名称
- `background_image`: 背景图片URL
- `menu_data`: JSON格式的菜单数据
- `created_at`: 创建时间
- `updated_at`: 更新时间

#### 分类表 (categories)
- `id`: 主键
- `name`: 分类名称
- `key`: 分类键值
- `color`: 分类颜色
- `created_at`: 创建时间

#### 预设表 (presets)
- `id`: 主键
- `name`: 预设名称
- `data`: 预设数据（JSON）
- `created_at`: 创建时间

## 🎨 主题系统

系统内置多个主题，支持自定义配色：

- **prosperity**: 开门红主题（喜庆红色）
- **spring**: 春天主题（清新绿色）
- **summer**: 夏天主题（活力蓝色）
- **autumn**: 秋天主题（温暖橙色）
- **winter**: 冬天主题（优雅银色）

## 🔧 API接口

### 菜单管理
- `GET /api/menu/current` - 获取当前菜单
- `POST /api/menu` - 创建/更新菜单
- `GET /api/menu/history` - 获取历史菜单

### 分类管理
- `GET /api/categories` - 获取所有分类
- `POST /api/categories` - 创建分类
- `PUT /api/categories/:id` - 更新分类
- `DELETE /api/categories/:id` - 删除分类

### 预设管理
- `GET /api/presets` - 获取所有预设
- `POST /api/presets` - 创建预设
- `DELETE /api/presets/:id` - 删除预设

## 🎯 使用说明

### 前台展示
1. 访问主页面查看当日菜单
2. 系统自动显示当前日期的菜单
3. 支持主题切换和背景图片显示

### 后台管理
1. 访问 `/admin/` 进入管理界面
2. 管理菜品和分类
3. 设置显示主题和背景
4. 保存和应用预设配置

## 🔄 缓存控制

系统实现了多级缓存控制：
- HTTP缓存头设置防止浏览器缓存
- JavaScript版本控制确保样式更新
- 数据库缓存提升查询性能

## 🌐 浏览器兼容性

- Chrome 60+
- Firefox 55+
- Safari 12+
- Edge 79+

## 📝 更新日志

### v1.0.0
- 初始版本发布
- 支持基本的菜单展示和管理功能
- 内置多个主题和自定义分类

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 📞 联系方式

如有问题或建议，请通过以下方式联系：
- 提交 Issue
- 发送邮件至 [your-email@example.com]

---

⭐ 如果这个项目对您有帮助，请给我们一个星标！